Console one-thread example: simple longpolling demo
Please create INI file in app folder (tg_longpolling.ini)
```ini
[Telegram]
;; Telegram token of the bot
Token=
;; Endpoint (for proxy if telegram server is blocked)
EndPoint=
```